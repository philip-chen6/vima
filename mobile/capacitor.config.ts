import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.vima.mobile",
  appName: "VIMA",
  webDir: "out",
  ios: {
    scheme: "VIMA",
  },
};

export default config;
